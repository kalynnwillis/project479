# Placeholder: train win probability model
source("R/utils.R")

message("Building win probability model...")
# model <- train_wp_model(data_raw)
# saveRDS(model, file = "data/interim/wp_model.rds")
message("WP model step complete.")
