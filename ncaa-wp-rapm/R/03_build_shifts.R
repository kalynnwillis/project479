# Placeholder: build player shifts from play-by-play and model
source("R/utils.R")

message("Building shifts...")
# shifts <- build_shifts(data_raw, model)
# saveRDS(shifts, file = "data/interim/shifts.rds")
message("Shifts building step complete.")
