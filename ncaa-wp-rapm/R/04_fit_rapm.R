# Placeholder: fit RAPM using regularized regression
source("R/utils.R")

message("Fitting RAPM...")
# rapm_results <- fit_rapm(shifts)
# saveRDS(rapm_results, file = "data/processed/rapm_results.rds")
message("RAPM fitting step complete.")
