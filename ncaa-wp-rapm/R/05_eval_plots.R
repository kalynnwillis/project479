# Placeholder: evaluation plots and tables
source("R/utils.R")

message("Generating evaluation plots...")
# rapm_results <- readRDS("data/processed/rapm_results.rds")
# p <- plot_rapm(rapm_results)
# ggsave(filename = "figs/rapm_plot.png", plot = p, width = 8, height = 5, dpi = 300)
message("Evaluation step complete.")
